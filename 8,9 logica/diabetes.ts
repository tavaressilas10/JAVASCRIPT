let nome, peso, altura, datainter, simenao
nome = prompt("Olá qual é o seu nome ?");
peso = prompt("Qual é o seu peso ?");
altura = prompt("Qual é a sua altura ?");
datainter = prompt("Qual foi a data da sua internação ?");
simenao = prompt("Você já foi internado ? ")

alert(nome + " seu peso é " + peso + "\n sua altura é  " + altura + " \nsua da de internação é " + datainter + " \ninternado ? " + simenao )


/*Indique o tipo de variável dos dados abaixo:

nome: string
sobrenome: string
peso: number
altura: number 
dataDeInternacao:string
diabético: string   */
